<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.envoss.com.calculatorbalok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LingkaranActivity" />
        <activity android:name=".HasilActivity" />
        <activity
            android:name=".BookActivity"
            android:label="Daftar Buku" />
        <activity android:name=".ExplicitIntentActivity" />
        <activity
            android:name=".DetailActivity"
            android:label="Detail Buku" />
        <activity
            android:name=".quiz.BoardActivity"
            android:label="Papan Score" />
        <activity
            android:name=".quiz.QuizActivity"
            android:label="Quiz" />
        <activity
            android:name=".fragment.FragmentActivity"
            android:label="Fragment Sederhana" />
        <activity android:name=".fragment.pager.PagerActivity">

        </activity>
        <activity android:name=".uts.RegisterActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:theme="@style/AppTheme.NoActionBar"
            android:name=".uts.ToolsActivity">

        </activity>
    </application>

</manifest>